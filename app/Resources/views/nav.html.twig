<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ path('g5_home_homepage') }}">Movie Nightmare</a>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-left">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-film fa-lg"></i> Movies <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><a href="{{ path('g5_movie_new') }}">New</a></li>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              {# <li class="divider"></li>
              <li><a href="#"><i class="fa fa-medkit"></i> Manager</a></li> #}
            {% endif %}
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-cogs fa-lg"></i> Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              <li><a href="{{ path('fos_user_security_logout') }}"><i class="fa fa-lg fa-sign-out"></i> Logout</a></li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_ANONYMOUSLY') and not is_granted('IS_AUTHENTICATED_FULLY') %}
              <li><a href="{{ path('fos_user_security_login') }}"><i class="fa fa-sign-in"></i> Login</a></li>
              <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              <li class="divider"></li>
              <li><a href="{{ path('g5_movie_tools_index') }}"><i class="fa fa-medkit"></i> Tools</a></li>
            {% endif %}
          </ul>
        </li>
      </ul>
      {# <form class="navbar-form navbar-right">
        <input type="text" class="form-control" placeholder="Search...">
      </form> #}
    </div>
  </div>
</div>
