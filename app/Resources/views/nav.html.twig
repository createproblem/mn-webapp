<a href="https://github.com/createproblem"><img style="position: absolute; top: 0; right: 0; border: 0; z-index: 9999;" src="https://s3.amazonaws.com/github/ribbons/forkme_right_red_aa0000.png" alt="Fork me on GitHub"></a>
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <button data-target=".nav-collapse" data-toggle="collapse" class="btn btn-navbar" type="button">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="{{ path('g5_home_homepage') }}" class="brand"><small>Movie Nightmare</small></a>
            <div class="nav-collapse collapse">
                <ul class="nav">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li class="active"><a href="{{ path('g5_movie_index') }}">Movies</a></li>
                    {% endif %}
                    <li class="dropdown">
                        <a data-toggle="dropdown" class="dropdown-toggle" href="#">Account <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                <li>
                                    <a href="{{ path('fos_user_security_logout') }}">
                                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                                    </a>
                                </li>
                                <li class="divider"></li>
                                <li class="nav-header">Profile</li>
                                <li><a href="#">Settings</a></li>
                            {% else %}
                                <li>
                                    <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|
                                        trans({}, 'FOSUserBundle') }}
                                    </a>
                                </li>
                                <li>
                                    <a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|
                                        trans({}, 'FOSUserBundle') }}
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    </li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>
