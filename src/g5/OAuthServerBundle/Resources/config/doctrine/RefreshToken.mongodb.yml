g5\OAuthServerBundle\Document\RefreshToken:
    type: document

    fields:
        id:
            id: true

    referenceOne:
        client:
            targetDocument: Client
        user:
            targetDocument: g5\AccountBundle\Document\User

